version: "3.7"

services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      DYNO: 'true'
  worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    environment:
      DYNO: 'true'
      REDIS_URL: 'redis'
    depends_on:
      - redis
    links:
      - redis
  redis:
    image: redis
