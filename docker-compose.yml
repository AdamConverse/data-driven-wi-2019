version: "3.7"

services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      DYNO: 'true'
    volumes:
      - "./app:app"
  worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    environment:
      DYNO: 'true'
      REDIS_URL: 'redis'
    depends_on:
      - redis
    links:
      - redis
    volumes:
      - "./app:app"
  redis:
    image: redis
